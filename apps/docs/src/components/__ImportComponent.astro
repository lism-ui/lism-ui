---
import { Text } from '@lism-ui/astro';

interface Props {
    componentName: string;
}
import { Preview, PreviewArea, PreviewTitle, PreviewCode } from '~/components/Preview';
---

<Preview>
    <PreviewTitle>import</PreviewTitle>
    <PreviewCode slot='tab' label='React'>```jsx ```</PreviewCode>
</Preview>
