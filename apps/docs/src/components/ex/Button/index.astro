---
import { Lism } from '@lism-ui/astro';

const { variant = 'fill', ...props } = Astro.props;

let lismClass = 'b--button';
if (variant) lismClass += ` b--button--${variant}`;
---

<Lism lismClass={lismClass} tag='a' hov='fade' {...props}>
	<slot />
</Lism>
