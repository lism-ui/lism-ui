---
title: 'Decorator'
description: ...
---

import { Decorator, Box, Flex, FlexItem, Grid, GridItem, Spacer, DammyText } from '@lism-ui/astro';
import { Reference, MemoBadge, HelpText } from "~/components";
import { Preview, PreviewArea, PreviewTitle, PreviewCode } from "~/components/Preview";
import ImportCode from "~/components/code-template/Import.astro";


コンテンツを装飾するための空要素を出力するためのコンポーネントです。  
デフォルトで`aria-hidden="true"`で出力されます。


## 構造
```pug
div.a--decorator[aria-hidden="true"]
```



## Props

| プロパティ | 説明 |
|---|---|
|`variant`| `a--decorator--{variant}` クラスを出力します。|
|`size`| デコレーターのサイズ（横幅・高さ）を一括指定します。<br/>この指定がある場合、`aspect='1'`が自動で付与され、指定したサイズは`w`に渡されます。|



## Import

<ImportCode component='Decorator' />


## Usage

`<Decorator>`を使った装飾例を紹介します。

<Preview>
	<PreviewTitle>Call</PreviewTitle>
	<PreviewArea resize p={20}>
		<Grid ta='l'>
			<Decorator
				size='1.25em'
				bd='l'
				rotate='45deg'
				transformOrigin='left bottom'
			/>
			<Box p='box:s'>
				<DammyText />
			</Box>
			<Decorator
				size='1.25em'
				bd='l'
				rotate='-45deg'
				transformOrigin='left top'
			/>
		</Grid>
	</PreviewArea>
	<PreviewCode slot="tab" label="JSX">
		```jsx
		<Grid ta='l'>
			<Decorator
				size='1.25em'
				bd='l'
				rotate='45deg'
				transformOrigin='left bottom'
			/>
			<Box p='box:s'>
				<DammyText />
			</Box>
			<Decorator
				size='1.25em'
				bd='l'
				rotate='-45deg'
				transformOrigin='left top'
			/>
		</Grid>
		```
	</PreviewCode>
	<PreviewCode slot="tab" label="HTML">
	```jsx
	```
	</PreviewCode>
</Preview>

<Spacer h='20' />

この他にも、以下のページで`<Decorator>`を活用したコンテンツの装飾例を複数紹介しています。

<Reference>
[装飾ボックスの例](/ja/components/decorations)
</Reference>
