---
title: 'Lism'
description: ...
---
import { Lism } from "@lism-ui/astro";
import { Reference, MemoBadge, HelpText, DammyText } from "~/components";
import { Preview, PreviewArea, PreviewTitle, PreviewCode } from "~/components/Preview";

Lism Props( [CSS Props](/docs/props/css-props) 及び [State Props](/docs/props/state-props) )を受け取るための特殊なコンポーネントです。

{/* 他のライブラリで作成されたコンポーネントに対して Lism Props を使用したい場合などにも利用できます。 */}

何も指定がなければ、ただの`<div>`タグを返します。

## Props

| プロパティ | 説明 |
|---|---|
|`tag`| 出力する HTMLタグを指定できます。デフォルトは`div`を返します。|
|`as`| Lism Propsの処理後、asに指定された別コンポーネントとして展開できます。 |
|`asProps`| Lism Propsとして処理したくないpropsを指定できます。`as`で指定した外部コンポーネントに渡したいpropsを指定するのに便利です。|
|`skipState`| State Props用の処理をスキップできます。|



{/* <Preview>
	<PreviewTitle>対応するHTML構造</PreviewTitle>
	<PreviewCode>
		```jsx
		<div>...</div>
		```
	</PreviewCode>
</Preview>

## Props
- `as`: 展開コンポーネント
- `tag`: HTMLタグ */}

## Import

```js
import { Lism } from '@lism-ui/core';
```

## Usage

<Preview>
	<PreviewTitle>Lismの例</PreviewTitle>
	<PreviewArea resize p={20}>
		<Lism isFlow p={30} bgc='base-2'>
			<p>Content</p>
			<p>Content</p>
			<p>Content</p>
		</Lism>
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Lism isFlow p={30} bgc='base-2'>
			<p>Content</p>
			<p>Content</p>
			<p>Content</p>
		</Lism>
		```
	</PreviewCode>
</Preview>
