---
title: 'Note'
description: ...
---
import { Icon, Box, DammyText } from '@lism-ui/astro';
import { Reference, MemoBadge, HelpText } from '~/components';
import { Preview, PreviewArea, PreviewTitle, PreviewCode } from '~/components/Preview';
import {Note} from '~/components/ex/Note';


ブログ記事などで、重要なポイントを示すためのコンポーネントです。


<Preview>
	<PreviewTitle>
		使用例
	</PreviewTitle>
	<PreviewArea p='30'>
		<Note title='Note Title'>
			<DammyText lang='ja'/>
		</Note>
	</PreviewArea>
	<PreviewCode>
	```jsx
	<Note title='Note Title'>
		<DammyText lang='ja'/>
	</Note>
	```
	</PreviewCode>
</Preview>


## コンポーネントの準備

<PreviewTitle>構築例</PreviewTitle>
<Preview>
	<PreviewCode slot='tab' label='React'>
	```jsx
	import { Lism, Flex, Stack, Icon, Center } from '@lism-ui/core';
	import notePresets from '@lism-ui/core/components/Callout/presets.js';

	export function Note({ type = 'note', boxcolor, icon, title, children, isFlow, ...props }) {
		const presets = type ? notePresets[type] : null;
		if (presets) {
			boxcolor = boxcolor || presets?.color || null;
			icon = icon || presets?.icon || null;
		}

		return (
			<Stack className='c--note' boxcolor={boxcolor} p='box' gap='20' bdrs='xs' {...props}>
				{title && (
					<Flex className='c--note__head' fw='bold' ai='c' gap='em4' skipState>
						<Center className='c--note__icon' fz='xl' c='boxcolor' skipState>
							<Icon icon={icon} />
						</Center>
						<span className='c--note__title'>{title}</span>
					</Flex>
				)}
				<Lism className='c--note__body' isFlow={isFlow}>
					{children}
				</Lism>
			</Stack>
		);
	}
	```
	</PreviewCode>
	<PreviewCode slot='tab' label='CSS'>
	```scss
	:where(.c--note) {
		border-inline-start: solid 4px var(--boxcolor);
	}
	```
	</PreviewCode>
</Preview>



### Props

| プロパティ | 説明 |
|---|---|
|`boxcolor`| 表示カラーを指定します。|
|`icon`| アイコンを指定できます。|
|`type`|`boxcolor`と`icon`の組み合わせのプリセット値を指定できます。初期値:`"note"`|
|`title`|タイトルテキストを指定します。|
|`isFlow`|`__body`に渡されます|
{/* - `variant`: `'fill'` | `'outline'` ? */}



### Usage

<Preview>
	<PreviewTitle>
		`icon`、`boxcolor`を指定
	</PreviewTitle>
	<PreviewArea p='30'>
		<Note title='Heading text...' icon='star' boxcolor='blue'>
			<DammyText lang='ja'/>
		</Note>
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Note title='Heading text...' icon='note'>
			<DammyText lang='ja'/>
		</Note>
		```
	</PreviewCode>
</Preview>

<HelpText>
※: アイコンは、`title` の指定がある時のみ表示されます。
</HelpText>


{/* <Preview>
	<PreviewTitle>
		Astroで`slot`を利用する例
	</PreviewTitle>
	<PreviewArea p='30'>
		<Note boxcolor='purple'>
			<Fragment slot='title'>Haeding Title</Fragment>
			<Icon tag='svg' slot='icon' viewBox="0 0 256 256">
				<path d="M208,24H72A32,32,0,0,0,40,56V224a8,8,0,0,0,8,8H192a8,8,0,0,0,0-16H56a16,16,0,0,1,16-16H208a8,8,0,0,0,8-8V32A8,8,0,0,0,208,24Zm-8,160H72a31.82,31.82,0,0,0-16,4.29V56A16,16,0,0,1,72,40H200Z"></path>
			</Icon>
			<DammyText lang='ja'/>
		</Note>
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Note boxcolor='purple'>
			<Fragment slot='title'>Haeding Title</Fragment>
			<Icon tag='svg' slot='icon' viewBox="0 0 256 256">
				<path d="M208,24H72A32,32,0,0,0,40,56V224a8,8,0,0,0,8,8H192a8,8,0,0,0,0-16H56a16,16,0,0,1,16-16H208a8,8,0,0,0,8-8V32A8,8,0,0,0,208,24Zm-8,160H72a31.82,31.82,0,0,0-16,4.29V56A16,16,0,0,1,72,40H200Z"></path>
			</Icon>
			<DammyText lang='ja'/>
		</Note>
		```
	</PreviewCode>
</Preview> */}


#### Presets

上記の構築例では、`<Callout>`と同じように、`type`を指定できるようにしています。

<Preview>
	<PreviewArea p='30'>
		<Note title='type="note"' type='note'>
			<DammyText length='codes' lang='ja'/>
		</Note>
		<Note title='type="alert"' type='alert'>
			<DammyText length='codes' lang='ja'/>
		</Note>
		<Note title='type="warning"' type='warning'>
			<DammyText length='codes' lang='ja'/>
		</Note>
		<Note title='type="point"' type='point'>
			<DammyText length='codes' lang='ja'/>
		</Note>
		<Note title='type="check"' type='check'>
			<DammyText length='codes' lang='ja'/>
		</Note>
		<Note title='type="info"' type='info'>
			<DammyText length='codes' lang='ja'/>
		</Note>
		<Note title='type="help"' type='help'>
			<DammyText length='codes' lang='ja'/>
		</Note>
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Note title='type="note"' type='note'>...</Note>
		<Note title='type="alert"' type='alert'>...</Note>
		<Note title='type="warning"' type='warning'>...</Note>
		<Note title='type="point"' type='point'>...</Note>
		<Note title='type="check"' type='check'>...</Note>
		<Note title='type="info"' type='info'>...</Note>
		<Note title='type="help"' type='help'>...</Note>
		```
	</PreviewCode>
</Preview>
