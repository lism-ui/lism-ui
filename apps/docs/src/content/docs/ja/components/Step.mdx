---
title: 'Step'
description: ...
---


import { Text, Badge, Lism, Flex, Stack, Icon, Center } from '@lism-ui/core';
import { Reference, MemoBadge, HelpText, DammyText } from '~/components';
import { Preview, PreviewArea, PreviewTitle, PreviewCode } from '~/components/Preview';
import {Question,ChatCircleDots} from '@phosphor-icons/react';
import { ArrowFatLinesDown } from '@phosphor-icons/react';
import { Step, StepBody, StepHead } from '~/components/ex/Step';

手順を示すレイアウトの構築例を紹介します。


## 構築例

<Preview>
<PreviewTitle>コンポーネントの準備</PreviewTitle>
<PreviewCode slot='tab' label='React'>
```jsx
import { Lism, Flex, Stack, Badge } from '@lism-ui/core';

export function Step(props) {
	return <Stack className='c--step' gap='em8' bdrs='m' bxsh='3' p='em12' {...props} />;
}

export function StepBody(props) {
	return <Lism isFlow='s' pis='em4' {...props} />;
}

export function StepHead({ num = '1', label = '', children, ...props }) {
	return (
		<Flex className='c--stepHead' gap='em8' ai='c' {...props}>
			<Badge variant='fill' tag='div' lh='1' px='em6' py='em3' bdrs='full'>
				{label && (
					<Lism tag='span' fz='xs' fs='i'>
						{label}
					</Lism>
				)}
				<Lism tag='span' fz='2xl' ff='mono' fs='i'>
					{num}
				</Lism>
			</Badge>
			<Lism fw='bold'>{children}</Lism>
		</Flex>
	);
}
```
</PreviewCode>
{/* <PreviewCode slot='tab' label='Astro'>
```jsx
```
</PreviewCode> */}
</Preview>

<Preview>
	<PreviewTitle>使用例</PreviewTitle>
	<PreviewArea resize p={30}>
		<Stack gap='40'>
			<Step bgc='base'>
				<StepHead tag='h3' num='1' label='Step.'>
					<>手順1のタイトル</>
				</StepHead>
				<StepBody>
					<p>
						ロレム・イプサムの座り雨、トマ好き学習エリット、しかし時と活力、そのような躍動と楽しみ、ブラインド行うにはいくつかの重要な事柄に座ります。
					</p>
				</StepBody>
			</Step>

			<Center>
				<Icon icon={ArrowFatLinesDown} weight='fill' fz='3xl' />
			</Center>

			<Step bgc='base'>
				<StepHead tag='h3' num='2' label='Step.'>
					<>手順2のタイトル</>
				</StepHead>
				<StepBody>
					<DammyText lang='ja' length='s' />
					<DammyText lang='ja' length='codes' />
				</StepBody>
			</Step>

			<Center>
				<Icon icon={ArrowFatLinesDown} weight='fill' fz='3xl' />
			</Center>

			<Step bgc='base'>
				<StepHead tag='h3' num='3' label='Step.'>
					<>手順3のタイトル</>
				</StepHead>
				<StepBody>
					<DammyText lang='ja' length='l' />
				</StepBody>
			</Step>
		</Stack>
	</PreviewArea>
	<PreviewCode>
```jsx
import { ArrowFatLinesDown } from '@phosphor-icons/react';
import { Stack, Center, AccordionIcon } from '@lism-ui/core';
import { Step, StepHead, StepBody } from './path/to/.../steps';

<Stack gap='40'>
		<Step>
			<StepHead num='1' label='Step.'>
				手順1のタイトル
			</StepHead>
			<StepBody>
				<p>
					ロレム・イプサムの座り雨、トマ好き学習エリット、しかし時と活力、そのような躍動と楽しみ、ブラインド行うにはいくつかの重要な事柄に座ります。
				</p>
			</StepBody>
		</Step>

		<Center>
			<Icon icon={ArrowFatLinesDown} weight='fill' fz='3xl' />
		</Center>

		<Step>
			<StepHead num='2' label='Step.'>
				手順2のタイトル
			</StepHead>
			<StepBody>
				<DammyText lang='ja' length='s' />
				<DammyText lang='ja' length='codes' />
			</StepBody>
		</Step>

		<Center>
			<Icon icon={ArrowFatLinesDown} weight='fill' fz='3xl' />
		</Center>

		<Step>
			<StepHead num='3' label='Step.'>
				手順3のタイトル
			</StepHead>
			<StepBody>
				<DammyText lang='ja' length='l' />
			</StepBody>
		</Step>
	</Stack>
```
	</PreviewCode>
</Preview>
