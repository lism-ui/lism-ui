---
title: 'CSS Props'
description: ...
sidebar:
  order: 3
---
import { Box, Grid } from "@lism-ui/astro";
import { DammyText, Reference, MemoBadge, HelpText } from "~/components";
import { Preview, PreviewArea, PreviewTitle, PreviewCode } from "~/components/Preview";


[Prop Utility](/ja/css/utility-class/) に対応するLism Propsを紹介します。

<Preview>
	<PreviewTitle>CSS Propsを指定する例</PreviewTitle>
	<PreviewCode>
	```jsx
	<Box c="black" bgc="white" p={20} fz="xl">...</Box>
	```
	</PreviewCode>
</Preview>

{/* <Note heading="Lism Propsの重要なポイント" type="point">
- プロパティと値の組み合わせによってはユーティリティクラス化されて出力される。
- ユーティリティクラス化されない時の挙動が数パターンある。
- ブレイクポイントで値を切り替えることができるものがある。
</Note> */}

{/* また、Lismコンポーネント以外では使用できませんので注意してください。 */}

## Colors

|Prop|CSS|Token|To Class|BP|
|---|---|---|---|---|
|`c`|`color`|`COLOR.PALETTE`, `COLOR.TXT`|`content`,`content-2`,`content-3`,`base`,`boxcolor`|-|
|`bgc`|`background-color`|`COLOR.PALETTE`, `COLOR.BG`|`base`,`base-2`,`base-3`,`content`,`t`,`i`,`cc`,`boxcolor`|-|
|`bdc`|`border-color`|`COLOR.PALETTE`, `COLOR.BD`|`divider`,`t`,`i`,`cc`,`boxcolor`|-|
|`boxcolor`|`--boxcolor`|`COLOR.PALETTE`|`COLOR.PALETTE`|-|


{/* ※ `bdc`は[`bd`との併用が必要](/docs/proips/border)です。 */}

<Reference>
[COLOR Tokens](/ja/css/tokens/)
</Reference>


## Typography

|Prop|CSS|Token|To Class|BP|
|---|---|---|---|---|
|f|font|-|`inherit`(`i`)|-|
|fz|font-size|`FONT.FZ`|`FONT.FZ`|○|
|lh|line-height|`FONT.LH`|`FONT.LH`|-|
|ff|font-family|`FONT.FF`|`FONT.FF`|-|
|lts|letter-spacing|`FONT.LTS`|`FONT.LTS`|-|
|ta|text-align|-|`center`(`c`),<br/>`left`(`l`),<br/>`right`(`r`)|-|
|td|text-decoration|-|`underline`(`u`),<br/>`none`(`n`)|-|
|whs|white-space|-|`nowrap`(`nw`)|-|
|ovw|overflowWrap|-|`anywhere`(`any`)|-|

{/* |font?|font|inherit?|-|-|
|td?|text-decoration|-|-|-| */}



## Width & Height

|Prop|CSS Property|Token|To Class|BP|
|---|---|---|---|---|
|w|width|`SIZE`|`100%`(`100`),<br/>`fit-content`(`fit`)|○|
|h|height|`SIZE`|`100%`(`100`),<br/>`fit-content`(`fit`),<br/>`screen`|○|
|maxW|max-width|`SIZE`|`100%`(`100`)|-|
|maxH|max-height|`SIZE`|`100%`(`100`)|-|
|minW|min-width|`SIZE`|`100%`(`100`)|-|
|minH|min-height|`SIZE`|`100%`(`100`)|-|

{/* max-widthは: s,m,l も? */}



## Margin and padding

|Prop|CSS|Token|To Class|BP|
|---|---|---|---|---|
|p|padding|`SPACE`|`SPACE`, `PADDING`, `0`|○|
|px|padding-inline|`SPACE`|`SPACE`, `0`|○|
|py|padding-block|`SPACE`|`SPACE`, `0`|○|
|pis|padding-inline-start|`SPACE`|`0`|○|
|pbs|padding-block-start|`SPACE`|`0`|○|
|pl|padding-left|`SPACE`|`0`|○|
|pr|padding-right|`SPACE`|`0`|○|
|pt|padding-top|`SPACE`|`0`|○|
|pb|padding-bottom|`SPACE`|`0`|○|
|m|margin|`SPACE`|`auto`|○|
|mx|margin-inline|`SPACE`|`auto`|○|
|my|margin-block|`SPACE`|`auto`|○|
|mis|margin-inline-start|`SPACE`|`auto`|○|
|mbs|margin-block-start|`SPACE`|`SPACE`, `auto`|○|
|ml|margin-left|`SPACE`|`auto`|○|
|mr|margin-right|`SPACE`|`auto`|○|
|mt|margin-top|`SPACE`|`auto`|○|
|mb|margin-bottom|`SPACE`|`auto`|○|


<Reference>
[SPACE Tokens](/ja/css/tokens/)
</Reference>

{/* Utility Class: 
- paddings
	- `-[p|px|py]:[0|5|10|15|20|30|40|50]`
	- `-p:box`, `-p:box:[s|l]`
- margins
	- `-[mis|mbs]:[0|5|10|15|20|30|40|50]`
	- `-[m|ml|mr|mt|mb|my|mx|mis|mbs]:auto` */}


{/* <Note>
論理プロパティは start方向 (pis, pbs, mis, mbs) だけでいいかどうかは要検討.<br/>
end方向はX,Yとstartの併用で擬似的に指定できるので、現状はstart方向のみ用意。
</Note> */}


<Preview>
	<PreviewTitle>Examples</PreviewTitle>
	<PreviewCode>
	```jsx
	<Box m={40}>...</Box>
	<Box pbs='20px'>...</Box>
	<Box mx='auto'>...</Box>
	<Box p='box' py={[null, 50]}>...</Box>
	<Text px={[20, 30]}>...</Text>
	```
	</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>各方向の値をp,mに対してオブジェクトで指定することも可能</PreviewTitle>
	<PreviewCode>
	```jsx
	<Box m={{l:20, b: 30}}>...</Box>
	<Box p={{x:30, y: 40}}>...</Box>
	```
	</PreviewCode>
</Preview>


## Displays

|Prop|CSS|Token|To Class|BP|
|---|---|---|---|---|
|d|display|-|`n`,`b`,`f`,`g`,`i`,`ib`,`if`,`ig`|○|
|v|visibility|-|`hidden`(`h`), `visible`(`v`)||
|op|opacity|`OPACITY`|`OPACITY`||
|`bxsh`|box-shadow|`SHADOW`|`SHADOW`||
|`bdrs`|border-radius|`RADIUS`|`RADIUS`||
|ov|overflow|-|`hidden`(`h`),`auto`(`a`),`clip`(`c`)|
|ovx|overflow-x|-|`hidden`(`h`),`auto`(`a`),`clip`(`c`)|
|ovy|overflow-y|-|`hidden`(`h`),`auto`(`a`),`clip`(`c`)|

{/* `none`,`"block"`,`"flex"`,`"grid"`,`"inline"`,`"inline-block"`,`"inline-flex"`,`"inline-grid"` */}


## Borders

|Prop|CSS|Token|To Class|BP|
|---|---|---|---|---|
|`bd`|`border`|-|`n`,`l`,`r`,`t`,`b`,`is`,`ie`,`bs`,`be`, `x`,`y`||
|`bdc`|`border-color`|`COLOR.PALETTE`, `COLOR.BD`|`divider`,`t`,`i`,`cc`,`boxcolor`|-|
|`bdw`|`border-width`|-|-|-|
|`bds`|`border-style`|-|-|-|
{/* |`bdl`|`border-left`|-|-|-|
|`bdr`|`border-right`|-|-|-|
|`bdt`|`border-top`|-|-|-|
|`bdb`|`border-bottom`|-|-|-|
|`bdis`|`border-inline-start`|-|-|-|
|`bdbs`|`border-block-start`|-|-|-| */}

{/* さらに、以下のものを `borders` オブジェクト内で指定することができます。

|Prop|CSS|Token|To Class|BP|
|---|---|---|---|---|
|`l`|`border-left`||- |-|
|`r`|`border-right`||- |-|
|`t`|`border-top`||- |-|
|`b`|`border-bottom`||- |-|
|`is`|`border-inline-start`||- |-|
|`ie`|`border-inline-end`||- |-|
|`bs`|`border-block-start`||-|-|
|`be`|`border-block-end`||-|-| */}



<Reference>
borderに関する仕様は少し特殊になっています。詳しくは[borderページ](/ja/styles/border/)をご覧ください。
</Reference>




## Positions

|Prop|CSS|Token|To Class|BP|
|---|---|---|---|---|
|`pos`|`position`|-|`r`,`a`,`s`,`f`,`sticky`|-|
|`inset`|`inset`|`SPACE`|`0`,`center`|-|
|`t`|`top`|`SPACE`|`0`,`50`,`100`|-|
|`l`|`left`|`SPACE`|`0`,`50`,`100`|-|
|`r`|`right`|`SPACE`|`0`,`50`,`100`|-|
|`b`|`bottom`|`SPACE`|`0`,`50`,`100`|-|
|`z`|`z-index`|-|`-1`, `0`, `1`|-|

さらに、以下のものは `insets` オブジェクト内で指定することができます。

|Prop|CSS|Token|To Class|BP|
|---|---|---|---|---|
|`is`|`inset-inline-start`|`SPACE`|`0`|-|
|`ie`|`inset-inline-end`|`SPACE`|`0`|-|
|`bs`|`inset-block-start`|`SPACE`|-|-|
|`be`|`inset-block-end`|`SPACE`|-|-|


## Places


|Prop|CSS|To Class|BP|
|---|---|---|---|
|`ai`|`align-items`|`c`, `s`, `e`, `fs`, `fe`, `str`|-|
|`ac`|`align-content`|`c`, `s`, `e`, `fs`, `fe`, `sb`|-|
|`ji`|`justify-items`|`c`, `s`, `e`, `fs`, `fe`, `str`|-|
|`jc`|`justify-content`|`c`, `s`, `e`, `fs`, `fe`, `sb`|-|
|`pi`|`place-items`|-|-|
|`pc`|`place-content`|-|-|



|Prop|CSS|To Class|BP|
|---|---|---|---|
|`aslf`|`align-self`|`c`, `s`, `e`, `str`|-|
|`jslf`|`justify-self`|`c`, `s`, `e`, `str`|-|
|`pslf`|`place-self`|-|-|
|`ord`|`order`|`-1`, `0`, `1`|-|



## Grid & Flex

grid形、flex形のプロパティはそれぞれ `grid`, `flex` オブジェクト、もしくは専用のコンポーネントのみで指定できるようになっています。


### gap

以下のものは `grid`, `flex` オブジェクト内で指定することができます。

|Prop|CSS|Token|To Class|BP|
|---|---|---|---|---|
|`g`|`gap`|`SPACE`|`SPACE`|○|
|`rowg`|`row-gap`|`SPACE`|-|○|
|`colg`|`column-gap`|`SPACE`|-|○|


{/* <Preview>
	<PreviewTitle>Examples</PreviewTitle>
	<PreviewCode>
	```jsx
	<Flex g={40}>...</Flex>
	<Grid g="1rem">...</Grid>
	<Columns rowg={[15, 20]} colg={[20, 30]}>...</Columns>
	```
	</PreviewCode>
</Preview> */}

{/* <Preview>
	<PreviewTitle>各方向の値をgapに対してオブジェクトで指定することも可能？</PreviewTitle>
	<PreviewCode>
	```jsx
	<Columns g={{row:20, column:30}}>...</Columns>
	```
	</PreviewCode>
</Preview> */}



### grid

以下のものは `grid` オブジェクト内で指定することができます。

|Prop|CSS|To Class|BP|
|---|---|---|---|
|`gd`|`grid`|-|○|
|`gt`|`grid-template`||-|
|`gta`|`grid-template-areas`|-|○|
|`gtc`|`grid-template-columns`|`a1a`|○|
|`gtr`|`grid-template-rows`|`a1a`|○|
|`gaf`|`grid-auto-flow`|-|-|
|`gar`|`grid-auto-rows`|-|-|
|`gac`|`grid-auto-columns`|-|-|

{/* autoFlow, autoRows, autoCols */}
{/* area → fix: 'fix', left: 'l', right: 'r', center: 'c', top: 't', bottom: 'b' */}

{/* - `-gt:side:l`, `-gt:side:r` クラスは、（追加CSSファイルを読み込むと）`[dir='rtl']`時に方向が反転するようになっており、ltr言語でもrtl言語でも方向が一致するようになっています。 */}


<PreviewTitle>基本的なgrid系プロパティの使用例</PreviewTitle>
```jsx
<Lism d='grid' ai='center' grid={{gtc:'1fr 1fr', g:40}}>...</Lism>
```

<PreviewTitle>`Grid`コンポーネントでは、`grid`オブジェクトを挟まずにそのまま指定できます。</PreviewTitle>

```jsx
// Grid系のコンポーネントを使う場合は、gridオブジェクトは不要です。
<Grid ai='center'gtc='1fr 1fr' g={40}>...</Grid>
```
{/* <Grid gtc='1fr 1fr' g={40} ai='center'>
<Box>aaa<br/>bbb</Box>
<Box>aaa</Box>
<Box>aaa</Box>
</Grid> */}


### gridItem

以下のものは `gridItem` オブジェクト内で指定することができます。

|Prop|CSS|To Class|BP|
|---|---|---|---|
|`ga`|`grid-area`|-|-|
|`gc`|`grid-column`||○|
|`gr`|`grid-row`||○|
|`gcs`|`grid-column-start`||-|
|`grs`|`grid-row-start`||-|
|`gce`|`grid-column-end`|-|-|
|`gre`|`grid-row-end`|-|-|

{/* `start`,`end`,`center` */}

{/* - `sp2` = `span 2 / span 2` */}



<PreviewTitle>使用例</PreviewTitle>
```jsx
// gridItemオブジェクトで各プロパティを指定します。
<Lism gridItem={{gc:'2'}} aslf='end'>...</Lism>

// GridItemのコンポーネントを使う場合は、gridItemオブジェクトは不要です。
<GridItem gc='2' aslf='end'>...</GridItem>
```


### flex

以下のものは `flex` オブジェクト内で指定することができます。

|Prop|CSS|To Class|BP|
|---|---|---|---|
|`fxw`|`flex-wrap`|`w`, `nw`|○|
|`fxd`|`flex-direction`|`c`, `cr`, `r`, `rr`|○|


### flexItem

以下のものは `flexItem` オブジェクト内で指定することができます。

|Prop|CSS|To Class|BP|
|---|---|---|---|
|`fx`|`flex`|`1`|○|
|`fxsh`|`flex-shrink`|`0`, `1`|-|
|`fxg`|`flex-grow`|`0`, `1`|-|
|`fxb`|`flex-basis`|-|○|



{/* - c: center
- s: start
- e: end
- fs: flex-start
- fe: flex-end
- str: stretch
- sb: space-between
 */}

## backgrounds

|Prop|CSS|Token|To Class|BP|
|---|---|---|---|---|
|`bg`|`background`|-|`"grid"`, `"stripe"`, `"glass"`|-|
|`bgc`|`background-color`|`COLOR`|`COLOR`|-|

さらに、以下のものは `backgrounds` オブジェクト内で指定することができます。


|Prop|CSS|Token|To Class|BP|
|---|---|---|---|---|
|`color`|`background-color`|||-|
|`image`|`background-image`|||-|
|`size`|`background-size`|||-|
|`position`|`background-position`|||-|

{/* |gradient||||-| */}


{/* ## insets

|Prop|CSS|Token|To Class|BP|
|---|---|---|---|---|
|`inset`|`inset`|-|`0`,`center`|-|
 */}



## Others


|Prop|CSS|Token|To Class|BP|
|---|---|---|---|---|
|`bdrs`|`border-radius`|`RADIUS`||-|
|`bxsh`|`box-shadow`|`SHADOW`||-|

{/* shColor */}

{/* ## border-radius
## box-shadow */}

その他、下記のCSSプロパティは `css` というプロパティにオブジェクトとして指定することで出力できます。  
(Note: 以下に挙げているもの以外は、そのまま `style`に渡されます。)

|Context|Prop|CSS|Token|To Class|BP|
|---|---|---|---|---|---|
|css.listStyle|listStyle||`none`||
|css.transform|transform||`flipX`,`flipY`,`flipXY`||
|css.transformOrigin|transformOrigin||`lt`,`lb`,`rt`,`rb`||
|css.translate|translate||`-50`,`-50X`,`-50Y`,`-100X`,`-100Y`||
|css.rotate|rotate||`45`,`-45`,`90`,`-90`,`180`||
|css.scale|scale||||
|css.clipPath|scale||||
|css.filter|filter||||
|css.backdropFilter|backdropFilter||||

{/*
transform: { map: 1, style: 1, utils: 1, utilKey: 'trf' },
transformOrigin: { style: 1, utilKey: 'trfo', utils: 'origin' },
translate: { style: 1, utils: 1, utilKey: 'trslt' },
rotate: { style: 1, utils: 1 },
scale: { style: 1 },
clipPath: { style: 1 }, // cpp ?
// animation: { map:1, style: 1, presets: 1 },
filter: { style: 1, converter: 'filter' },
backdropFilter: { style: 1, utilKey: 'bdFilter', presets: ['blur'], converter: 'filter' }
*/}

## 対応トークンとユーティリティクラスでの出力について

各プロパティごとに、指定した値によってはユーティリティクラスとして出力される場合があります。

<Preview>
	<PreviewCode>
	```jsx
	<Box p={20} bxsh="2" ta="center" fz="xl">...</Box>
	```
	</PreviewCode>
</Preview>
出力結果は次のようになります。

```jsx
<div class="l--box -p:20 -bxsh:2 -ta:l">...</div>
```

具体的にどの値をユーティリティ化するかどうかの詳細は`/src/config/presets`,`/src/config/utilities`で定義しています。

utilities側で定義しているものは、キーと省略形のどちらを指定してもユーティリティクラスとして出力されます。

たとえば、`text-align:center`用の `-ta:c` クラスは、以下のどちらの書き方でも出力されます。
```jsx
<Box ta="center">...</Box>
<Box ta="c">...</Box>
```


### ユーティリティクラス化されない時の出力パターンについて

ユーティリティクラスとして出力されない時、3パターンの出力方式に分かれます。

{/* - memo...
- 1のメリット
    - 何かしらそのpropを持つかどうかがクラスで判定できる。
	- 子要素に変数の受け渡しをすることで、親では何も適用せずにinheritさせるみたいなことができる。
    - 優先度的な問題を緩和できる。
		- インラインセットは優先度が強いが、プロパティのセットはCSSファイル側で 0-1-0　の優先度で行われる。
		- 一括指定プロパティとサイドプロパティの優先度の関係をコントロールできる
    - →HTML側の文字数が少なくなる(ほんの少し)
- 1,2の判断基準どうする？
	- utility以外を使うことが珍しいか（自由な数値で値をとれるか）どうか?
	- ブレイクポイントで切り替えるかどうかで判断すればいいのでは？ */}

#### 1. `-prop:`クラスと`--prop`変数の組み合わせで出力される


<Preview>
	<PreviewCode label="例" slot="tab">
	```jsx
	<Box mb={40} fz='2em'>...</Box>
	```
	</PreviewCode>
	<PreviewCode label="出力" slot="tab">
	```jsx
	<div class="l--box -mb: -fz:" style="--mb:var(--s--40);--fz:2em">...</div>
	```
	</PreviewCode>
</Preview>


- ブレイクポイント対応のプロパティ（ベース値を上書きする必要があるため、インラインで直接プロパティを出力せずクラスと変数で優先度の問題をなくす）
- ユーティリティクラスが多く`[class*='-prop:']`にすると文字数の省略になりそうなもの
- `-{prop}:`クラスによって何か値を指定していることが分かったほうが便利なケースがありそうなプロパティ

**`-prop:`クラスだけを出力する方法**

このパターン1での出力がされるLism Propsに対して `"-"` または `true` を指定することで、`.-{prop}:` クラスだけの出力が可能です。

CSSファイル側で具体的な値を指定したい場合などに活用できたり、一部、`-{prop}:`クラスがスタイルを持っている特殊なケースもあります。

<Preview>
	<PreviewCode label="例" slot="tab">
	```jsx "bd" "'-'"
	<Box bd w='-' p={['-', '-']}>...</Box>
	```
	</PreviewCode>
	<PreviewCode label="出力" slot="tab">
	```html "-bd:" "-w:" "-p:" "-p@sm"
	<div class="l--box -bd: -w: -p: -p@sm">...</div>
	```
	</PreviewCode>
</Preview>



このように、`-{prop}:`クラスだけ出力されてCSS変数は何も出力されません。（パターン2で出力される`opacity`に関しては、`-opacity:`クラスが出力されていないことにも注目してください。）


#### 2. 単純にstyleとして出力されるもの


<Preview>
	<PreviewCode label="例" slot="tab">
	```jsx
	<Box z="10" opacity="0.5">...</Box>
	```
	</PreviewCode>
	<PreviewCode label="出力" slot="tab">
	```jsx
	<div class="l--box" style="z-index:10;opacity:0.5">...</div>
	```
	</PreviewCode>
</Preview>


#### 3. `--prop`だけ出力されるもの

このケースが一番稀です。

<Preview>
	<PreviewCode label="例" slot="tab">
	```jsx
	<Box bd bdc="#000" bdw="2px">...</Box>
	```
	</PreviewCode>
	<PreviewCode label="出力" slot="tab">
	```jsx
	<div class="l--box bd:" style="--bdc:#000;--bdw:2px">...</div>
	```
	</PreviewCode>
</Preview>


- `--bdc`,`--bdw`,`--bds` のborder系
{/* - `--keycolor` など、利用する場面が限定されているもの。 */}


## ユーティリティクラスとして強制的に出力する方法

`u:`からはじめると、その後ろの文字列がユーティリティクラス名として出力されます。

```jsx
<Box p='u:hoge'>...</Box>

↓

<div class="l--box -p:hoge">...</div>
```

