---
title: 'State Props'
description: ...
sidebar:
  order: 2
---
import { Box, Grid, Alert, Note } from "@lism-ui/astro";
import { DammyText, Reference, MemoBadge, HelpText } from "~/components";
import { Preview, PreviewArea, PreviewTitle, PreviewCode } from "~/components/Preview";


[Design State](/ja/css/design-state/) クラスに対応するプロパティ群を紹介します。


| Prop | 出力クラス |
|---|---|
| `isFlow` | `is--flow` |
| `isFlow='s'` | `is--flow:s` |
| `isFlow='l'` | `is--flow:l` |
| `isContainer` | `is--container` |
| `isContainer='s'` | `is--container:s` |
| `isContainer='m'` | `is--container:m` |
| `isContainer='l'` | `is--container:l` |
| `isWide` | `is--wide` |
| `isFullwide` | `is--fullwide` |
| `isOverwide` | `is--overwide` |
| `hasGutter` | `has--gutter` |
| `hasDivider` | `has--divider` |

