---
import { LismProps } from '../types';
import getLismProps from '@lism-ui/core/lib/getLismProps';
import { getAccProps } from '@lism-ui/core/components/Accordion/getProps';

// Propsの定義
interface Props extends LismProps {
	duration?: string | number;
}
const props = Astro.props || {};
---

<details {...getLismProps(getAccProps(props))}>
	<slot />
</details>

<script>
	// import setEvent from './setEvent';
	import setAccordion from '@lism-ui/core/components/Accordion/setAccordion';
	setAccordion();
</script>
