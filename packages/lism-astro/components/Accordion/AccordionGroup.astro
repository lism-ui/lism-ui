---
import { LismProps } from '../types';
import { Stack } from '../Stack';
import { getAccGroupProps } from '@lism-ui/core/components/Accordion/getProps';

interface Props extends LismProps {}
const props = Astro.props || {};
---

<Stack {...getAccGroupProps(props)}><slot /></Stack>
