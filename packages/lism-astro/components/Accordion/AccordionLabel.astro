---
import { LismProps } from '../types';
import { FlexItem } from '../Flex';
import { getAccLabelProps } from '@lism-ui/core/components/Accordion/getProps';

interface Props extends LismProps {}
const props = Astro.props || {};
---

<FlexItem {...getAccLabelProps(props)}>
	<slot />
</FlexItem>
