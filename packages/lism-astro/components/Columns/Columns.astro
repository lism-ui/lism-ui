---
import { LismProps } from '../types';
import { Grid } from '../Grid';
// import getProps from '@lism-ui/core/components/Columns/getProps';

// Propsの定義
interface Props extends LismProps {}
const props = Astro.props || {};
---

<Grid _grid='columns' {...props}><slot /></Grid>
