---
import type { LismProps } from '../types';
import { Lism } from '../Lism';
import getProps from '@lism-ui/core/components/Decorator/getProps';

// Propsの定義
interface Props extends LismProps {}
const props = Astro.props || {};
---

<Lism {...getProps(props)}>
	<slot />
</Lism>
