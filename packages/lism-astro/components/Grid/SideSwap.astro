---
import { LismProps } from '../types';
import Grid from './Grid.astro';
import { getSideSwapProps } from '@lism-ui/core/components/Grid/getProps';

// Propsの定義
interface Props extends LismProps {}
let props = Astro.props;
---

<Grid variant='swap' {...getSideSwapProps(props)}>
	<slot />
</Grid>
