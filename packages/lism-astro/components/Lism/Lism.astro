---
import { LismProps } from '../types';
import { getLismProps } from '@lism-ui/core/lib';

// Propsの定義
interface Props extends LismProps {}

const { as, passProps, tag, ...props } = Astro.props || {};
const JSX = as || tag || 'div';
---

<JSX {...getLismProps(props)} {...passProps}>
    <slot />
</JSX>
