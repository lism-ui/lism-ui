---
import { LismProps } from '../types';
import getLismProps from '@lism-ui/core/lib/getLismProps';
import getMediaProps from '@lism-ui/core/components/getMediaProps';

// Propsの定義
interface Props extends LismProps {
	// src?: string;
	// alt?: string;
	// width?: string | number;
	// height?: string | number;
}
const { as, exProps, tag, ...props } = Astro.props || {};

const JSX = as || tag || 'img';
---

<JSX {...getLismProps(getMediaProps(props))} {...exProps}>
	<slot />
</JSX>
