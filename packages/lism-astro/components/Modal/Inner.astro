---
import { LismProps } from '../types';
import { defaultProps } from '@lism-ui/core/components/Modal/getProps';
import { Stack } from '../Stack';

// Propsの定義
interface Props extends LismProps {}
const props = Astro.props || {};
---

<Stack {...defaultProps.inner} {...props}>
	<slot />
</Stack>
