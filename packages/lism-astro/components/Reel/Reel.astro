---
import { LismProps } from '../types';
import { Flex, Stack } from '../Flex';
import getReelProps from '@lism-ui/core/components/Reel/getProps';

interface Props extends LismProps {}
const { isVertical, ...props } = Astro.props || {};
const Layout = isVertical ? Stack : Flex;
---

<Layout {...getReelProps(props)}><slot /></Layout>
