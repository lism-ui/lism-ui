---
import { LismProps } from '../types';
import { Flex } from '../Flex';

export interface Props extends LismProps {}
const { lismClass = {}, ...props } = Astro.props;

lismClass.c = 'c--tabs__list';
// if (variant) lismClass.c += ` c--tabs__list--${variant}`;
---

<Flex lismClass={lismClass} jc='s' role='tablist' {...props}>
	<slot />
</Flex>
