---
import { LismProps } from '../types';
import { Lism } from '../Lism';
import getProps from '@lism-ui/core/components/List/getProps';

// Propsの定義
interface Props extends LismProps {}
const props = Astro.props || {};
---

<Lism tag='ul' {...getProps(props)}>
	<slot />
</Lism>
